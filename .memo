inbox処理

1.受信した手紙(@in) + 送信した手紙(@out) をDBからfind
2.以下に整形
 ・@inからuser_idを取得し、userごとに箱を作成
  ・箱ごとに対応するuser_idに@inと@outをhashで紐づける
3.viewに箱を描画
  ※処理次第ではajaxも視野


taskフロー

Task.Aが手紙を送信

Flow.to_user_idをBに設定しinsert
     A.新規の場合,matchをinsertしそれにぶら下げる
     B.返信の場合,対象matchにぶら下げる

Task.Bがinboxに遷移

Flow.対象matchとそれにぶら下がるmessageを全て取得

Task.Bが手紙を削除

Flow.対象match