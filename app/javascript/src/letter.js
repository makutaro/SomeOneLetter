// // 手紙の作成 => 確認ボタン押下で呼び出されるメソッド
// $('#previewModal').on('show.bs.modal', function (event) {

//   //formの値を取得(title,content,layout_id)
//   let title     = document.getElementById('letter_title');
//   let content   = document.getElementById('letter_content');
//   let layout_id = document.getElementById('letter_layout_id');

//   // modalにパラーメータを渡す
//   var modal = $(this);
//   modal.find('#preview-modal').val(title,content,layout_id);
// })
